# Wird beim Steam-Build (npm run build:win) eingebacken -- vor Upload auf
# die echte Server-Adresse setzen (siehe docs/ROADMAP.md Abschnitt 5).
VITE_API_BASE_URL=https://cookie.r3dconcrete.de
VITE_WS_URL=wss://cookie.r3dconcrete.de/ws-market

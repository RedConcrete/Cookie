package cookie.server.repository;

import cookie.server.entity.SkillNodeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SkillNodeRepository extends JpaRepository<SkillNodeEntity, String> {
}
